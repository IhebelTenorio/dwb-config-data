server:
  port: 8084

system:
  hostname: localhost

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/dwb_login
    password: demo_123 
    username: dwb
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update

springdoc:
  swagger-ui:
    operations-sorter: method
    default-model-expand-depth: 5
    default-model-rendering: model
  packagesToScan: com.auth_service.api.controller
  pathsToMatch: /**
